# Data folder

- `raw/` contains original data downloads (do not modify).
- `processed/` contains cleaned/derived datasets generated during analysis.

Tips:

- Prefer CSV or Parquet for processed outputs.
- Save a small sample of raw if full files are too large for Git.
